# DocCollab - Веб-приложение для совместного редактирования документов

## 📋 Описание проекта

**DocCollab** - это курсовая работа по предмету "Разработка архитектуры веб-приложения для совместного редактирования и управления документами". Приложение позволяет нескольким пользователям одновременно редактировать текстовые документы в реальном времени.

### 🎯 Основные функции

- **Совместное редактирование**: Multiple пользователи могут редактировать один документ одновременно
- **Real-time синхронизация**: Мгновенное обновление изменений у всех участников
- **Operational Transformation**: Алгоритм разрешения конфликтов редактирования
- **Управление документами**: Создание, редактирование, удаление документов
- **Пользовательские аккаунты**: Регистрация, аутентификация, управление профилем
- **WebSocket соединение**: Эффективная real-time коммуникация

### 🏗️ Архитектура

Проект реализован с использованием **Domain-Driven Design (DDD)**:

```
app/
├── domains/           # Бизнес-логика
│   ├── identity/      # Управление пользователями
│   ├── documents/     # Управление документами
│   └── collaboration/ # Совместное редактирование
├── api/               # API эндпоинты
│   ├── http/          # REST API
│   └── ws/            # WebSocket
├── core/              # Ядро приложения
├── db/                # База данных
└── static/            # Frontend
```

### 🛠️ Технологический стек

**Backend:**
- **FastAPI** - современный веб-фреймворк
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy** - ORM с async поддержкой
- **Alembic** - миграции базы данных
- **WebSocket** - real-time коммуникация
- **JWT** - аутентификация
- **Pydantic v2** - валидация данных

**Frontend:**
- **Vanilla JavaScript** - без фреймворков
- **CodeMirror** - текстовый редактор
- **Bootstrap** - UI компоненты
- **WebSocket API** - real-time обновления

**DevOps:**
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **Uvicorn** - ASGI сервер

## 🚀 Быстрый запуск

### Требования
- Docker и Docker Compose
- 2GB свободной памяти

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd doccollab
```

2. **Настройте окружение:**
```bash
cp .env.example .env
```

3. **Запустите приложение:**
```bash
docker-compose up -d --build
```

4. **Примените миграции:**
```bash
docker-compose exec app alembic upgrade head
```

5. **Откройте в браузере:**
```
http://localhost:8080
```

## 📚 Документация

- **[Docker Setup Guide](DOCKER_SETUP_GUIDE.md)** - подробная инструкция по запуску
- **[User Guide](USER_GUIDE.md)** - руководство пользователя
- **[Architecture Analysis](architecture_analysis.md)** - анализ архитектуры
- **[UML Diagrams](uml_diagrams.md)** - UML диаграммы проекта

## 🔧 API Документация

После запуска приложения доступна автоматическая документация:
- **Swagger UI**: http://localhost:8080/docs
- **ReDoc**: http://localhost:8080/redoc

## 🧪 Тестирование

### Регистрация пользователя
```bash
curl -X POST http://localhost:8080/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "Password123"
  }'
```

### Вход в систему
```bash
curl -X POST http://localhost:8080/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Password123"
  }'
```

### Создание документа
```bash
curl -X POST http://localhost:8080/documents \
  -H "Authorization: Bearer <your-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Document",
    "content": "Hello, World!"
  }'
```

## 🏛️ Структура проекта

### Домены (Domains)

**Identity Domain** - Управление пользователями
- Регистрация и аутентификация
- Управление профилем
- JWT токены

**Documents Domain** - Управление документами
- CRUD операции с документами
- Версионирование
- Поиск и экспорт

**Collaboration Domain** - Совместная работа
- Operational Transformation
- WebSocket синхронизация
- Управление сессиями

### API Слой

**HTTP API** - RESTful эндпоинты
- `/auth/*` - аутентификация
- `/documents/*` - управление документами
- `/users/*` - управление пользователями
- `/collaboration/*` - совместная работа

**WebSocket API** - Real-time коммуникация
- `/ws/sync/{document_id}` - синхронизация документа

## 🔍 Особенности реализации

### Operational Transformation
Реализован алгоритм преобразования операций для разрешения конфликтов при одновременном редактировании:
- **Insert операции** - вставка текста
- **Delete операции** - удаление текста
- **Transform функция** - преобразование конфликтующих операций

### WebSocket Архитектура
- **Connection management** - управление подключениями
- **Message broadcasting** - рассылка изменений
- **Session tracking** - отслеживание активных пользователей

### Безопасность
- **JWT аутентификация** с expire временем
- **Password hashing** с bcrypt
- **CORS настройки** для frontend
- **Input validation** с Pydantic

## 🐛 Поиск проблем

### Основные команды
```bash
# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs app

# Перезапуск
docker-compose restart app

# Полная перезагрузка
docker-compose down -v && docker-compose up -d --build
```

### Частые проблемы
1. **Порт занят**: измените порт в docker-compose.yml
2. **База данных не доступна**: проверьте миграции
3. **Ошибка аутентификации**: проверьте JWT_SECRET в .env

## 📈 Производительность

### Оптимизации
- **Async/await** для всех операций с БД
- **Connection pooling** для PostgreSQL
- **Efficient WebSocket** сообщения
- **Minimal frontend** без тяжелых фреймворков

### Мониторинг
```bash
# Использование ресурсов
docker stats

# Здоровье системы
curl http://localhost:8080/health
```

## 🔮 Будущие улучшения

### Планируемые функции
- [ ] Подсветка синтаксиса кода
- [ ] История изменений документа
- [ ] Комментарии и упоминания
- [ ] Экспорт в различные форматы
- [ ] Мобильное приложение

### Технические улучшения
- [ ] Redis для кеширования сессий
- [ ] Микросервисная архитектура
- [ ] CI/CD пайплайн
- [ ] Мониторинг и логирование

## 📄 Лицензия

Проект создан в учебных целях для курсовой работы по направлению "Программная инженерия".

## 👥 Автор

**Студент 3 курса**  
Направление: "Программная инженерия"  
Дисциплина: "Разработка архитектуры веб-приложения для совместного редактирования и управления документами"

---

**Приложение доступно по адресу**: http://localhost:8080

**API документация**: http://localhost:8080/docs
