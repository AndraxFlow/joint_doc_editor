# Docker Configuration for DocCollab
# =====================================

# PostgreSQL Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=docpass
POSTGRES_DB=docdb

# Database URL (Docker internal network)
# Note: 'db' is the service name in docker-compose.yml
DATABASE_URL=postgresql+asyncpg://postgres:docpass@db:5432/docdb

# JWT Configuration
JWT_SECRET=supersecretkey_change_this_in_production
JWT_ALGORITHM=HS256

# Application Configuration
# APP_PORT=8000 (internal container port)
# HOST_PORT=8080 (external host port)
